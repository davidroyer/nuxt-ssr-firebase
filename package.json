{
  "name": "firebase-functions-nuxt-example",
  "version": "1.0.0",
  "description": "Host Nuxt.js SSR app on Firebase Cloud Functions with Firebase Hosting redirects.",
  "license": "MIT",
  "author": "David Royer",
  "repository": "",
  "dependencies": {
    "vuetify": "^0.15.1"
  },
  "scripts": {
    "setup": "yarn setup:client && yarn setup:server && yarn build && yarn setup:firebase",
    "setup:client": "cd \"src\" && yarn",
    "setup:server": "cd \"prod/server\" && yarn",
    "setup:firebase": "mv .setup-firebaserc .firebaserc",
    "setup:firebase2": "cp .firebaserc-d .firebaserc-d2",
    "fireserve": "firebase serve --only functions,hosting",
    "dev": "cd \"src\" && yarn nuxt",
    "dev-ssr": "yarn ssr & yarn dev",
    "build": "cd \"src\" && yarn nuxt build && yarn copyassets",
    "build:client": "cd \"src\" && yarn nuxt build && yarn copyassets",
    "build:server": "cd \"prod/server\" && yarn",
    "build:all": "yarn build:client && yarn build:server",
    "generate": "cd \"src\" && yarn nuxt generate",
    "prepare-deploy": "yarn build:client && firebase deploy",
    "deploy": "firebase deploy",
    "assets": "cd \"src\" && yarn copyassets",
    "ssr": "firebase serve --only functions"
  },
  "devDependencies": {
    "cross-env": "^5.0.5"
  }
}
