{
  "name": "firebase-functions-nuxt-example",
  "version": "1.0.0",
  "description": "Host Next.js SSR app on Firebase Cloud Functions with Firebase Hosting redirects.",
  "license": "MIT",
  "author": "David Royer",
  "repository": "",
  "dependencies": {},
  "scripts": {
    "setup": "yarn build:all",
    "fireserve": "firebase serve --only functions,hosting",
    "dev": "cd \"app/src\" && yarn nuxt",
    "build:client": "cd \"app/src\" && yarn nuxt build",
    "build:server": "cd \"app/functions\" && yarn",
    "build:all": "yarn build:client && yarn build:server",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  }
}
